## name = testgui
## title = GUI test
## description = used to test embedding ui files
## subroutine = run
## menu = &File

from PyQt4.QtCore import *
from PyQt4.QtGui import *
from PyQt4.uic import loadUi

import mikro
import __main__

class GuiTest :

  def quit(self) :
    self.window.close()
    

  def __init__(self):
    #app = QApplication(sys.argv)


    script_path = "/home/ale/docs/src/scribus-scripter-repository/gui_test/gui_test.ui";
    self.window = loadUi(script_path)
    self.window.actionQuit.triggered.connect(self.quit)
    self.window.show()
    while self.window.isVisible():
        qApp.processEvents()

def run():
    guiTest = GuiTest()
